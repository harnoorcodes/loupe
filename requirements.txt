# Milestone 0 -- connectivity only. Grows in later milestones.
openai-agents>=0.6.0      # the Agents SDK (Agent, Runner, handoffs, tools)
openai>=1.99.0            # HTTP client; we point it at Gemini's endpoint
pydantic>=2.9.0           # typed outputs
pydantic-settings>=2.5.0  # env-var config
python-dotenv>=1.0.1      # loads .env